{
  "type": "object",
  "properties": {
    "requiresApproval": {
      "type": "boolean",
      "title": "Requires Approval Workflow",
      "default": false
    },
    "workflowId": {
      "type": "string",
      "title": "Workflow ID"
    },
    "filePicker": {
      "type": "string",
      "format": "data-url",
      "title": "Upload Document"
    }
  },
  "required": ["filePicker"],
  "errorMessage": {
    "required": {
      "filePicker": "File upload is required"
    }
  }
}
